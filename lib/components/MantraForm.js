/*
    MantraForm (Phase 1 - Installation): Draft

    Attributes

    - name: string (Refers to the component name)
    - fields: array<string> (Refers to the component's fields)

    Operations

    - N/A
*/

export default class MantraForm {
    constructor({ name, fields }) {
        this.name = name;
        this.fields = fields;
    }
}